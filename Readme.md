# Git Basics

## Git CLI & Remote

- `git init`
- `git status`
- `git add <filename>`
- `git commit -m "Commit message"`
- `git remote -v`
- `git remote add origin <ssh link>`
- `git push -u origin <branch-name>`

## Git Branches & PRs
